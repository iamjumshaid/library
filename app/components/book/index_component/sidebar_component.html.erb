<aside class="w-full px-4 py-5 block lg:w-96">
  <div class="pb-5">
    <div class="flex items-center justify-between">
      <h1 class="text-xl font-semibold leading-6 text-gray-800">Filters</h1>
      <p class="font-semibold text-gray-600 sm:ml-2 xl:ml-0">
        <%= results_count.to_s+' results' %>
      </p>
    </div>
    <div class="mt-5 pt-5 lg:mt-0">
      <%= render Book::IndexComponent::FilterComponent::CategoryComponent.new(categories: Category.joins(:books).where(books: books).uniq) %>
    </div>
  </div>
</aside>