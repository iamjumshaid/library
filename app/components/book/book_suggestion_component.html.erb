<a href="#" class="block hover:bg-gray-50 col-span-1 flex flex-col text-center bg-white rounded-lg shadow divide-y divide-gray-200 h-full">
  <div class="flex-1 flex flex-col p-8">
    <div class="border border-gray-200 rounded-full w-24 h-24 mx-auto">
      <%= outline_icon 'book-open', class: 'w-full p-6 flex-shrink-0 mx-auto rounded-full text-gray-200', 'aria-hidden': true %>
    </div>
    <h3 class="mt-6 text-gray-900 text-sm font-medium"><%= book.title %></h3>
    <dl class="mt-1 flex-grow flex flex-col justify-between">
      <dd class="text-gray-500 text-sm truncate"><%= book.description %></dd>
      <dd class="mt-3 px-2 py-1 text-green-800 text-xs font-medium bg-green-100 rounded-full">
        <span class="max-w-full break-all"><%= book.categories.first.name %></span>
      </dd>
    </dl>
  </div>
</a>