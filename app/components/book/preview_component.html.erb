<h1 class="text-xl font-semibold leading-6 text-gray-800">Preview Pages</h1>
<div class="container mx-auto space-y-2 lg:space-y-0 lg:gap-1 lg:grid lg:grid-cols-3 b-t">
  <% book.previews.each do |page| %>
    <div class="w-2/3 rounded hover:shadow-2xl">
      <%= image_tag page %>
    </div>
  <% end %>
</div>